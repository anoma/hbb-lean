# Capped HBB4 CM Implementation Plan

> Execute with the subagent-driven-development workflow; compile every completed dependency before proving downstream theorems.

**Goal:** Formalize the finite capped CM protocol, a live finite model, provenance, agreement, and both liveness results.

**Architecture:** Retain the existing finite Model and restricted ThyLive. Define the exact maximum correlation-row chain length, then the protocol rules and semantic lemmas. Use causal induction for provenance and conflict-depth, and bounded round induction for progress.

**Tech Stack:** Existing Lean 4.24.0-rc1 toolchain and library; no added dependencies.

**Spec:** `audits/hbb4_cm/INVESTIGATION.md`, and Sections 3–5 of the revised HBB4 PDF.

## Constraints

No infinite histories, arbitrary replacement depth budget, custom logical axioms, or final `sorry`. Definitions are complete before theorem RED. Preserve the user's untracked PDF and existing HBB1–3 results.

## Tasks

- [x] `ThyHBB4/Depth.lean`: define valid distinct-row causal chains, prove their height bound, define their attained natural maximum, and prove the chain bound. Validate with `lake env lean`.
- [x] `ThyHBB4/Axioms.lean`: encode symbols, ranks, certificates, legality, capped forward/backward rules, CM and restricted ThyLive. Compile definitions and projections.
- [x] `ThyHBB4/FiniteModel.lean`: construct a completed finite live execution; prove the actual protocol assumptions and liveness antecedents.
- [x] `ThyHBB4/Safety.lean`: prove proposal provenance and conflict-depth by causal/rank induction, derive legality and agreement.
- [x] `ThyHBB4/Liveness.lean`: disseminate fixed-source certificates using existing Knowledge, prove capped round progression and both liveness statements.
- [x] Import completed modules into the default build; audit headline theorem axioms; update mapping/documentation; run `lake build` and commit task-owned changes.

Each theorem uses RED/GREEN on its production declaration. No fixture theorems stand in for the required mathematical claims.

## Validation

The default build includes the CM modules and guarded axiom checks for exact
depth attainment, provenance, conflict-depth, agreement, both liveness theorems,
and the finite nonvacuity witness. The witness uses eight events and singleton
sorts; the independent Python audit covers the 52-event distinct-learner case.
Independent reviews checked the source-rule correspondence, proof assumptions,
and finite model's arbitrary-list Knowledge argument.
