# Capped HBB4 SW and CW Implementation Plan

**Goal:** Prove agreement and both liveness properties for SW and CW, sharing the
finite capped protocol with CM and proving finite nonvacuity.

**Spec:** `hbb4_weaker_coherence.pdf`, Sections 1–4; retain the finite foundation,
restricted Knowledge, exact MaxDepth, corrected self-source rule and round cap.

- [x] Extract BaseProtocol, including participant-local correlation persistence;
  migrate CM to extend it and generalize coherence-independent proofs.
- [x] Define exact SW and CW event conditions and prove CM ⇒ SW ⇒ CW.
- [x] Prove CW conflict-depth with strictly nested row chains, then legality at
  rank MaxDepth+1 and agreement. Derive SW by its proved CW implication.
- [x] Share the liveness proofs, supplying each variant's proved legality lemma.
- [x] Lift the kernel-checked finite witness to SW/CW, with actual liveness premises.
- [x] Build all theories, audit headline proof axioms, review source correspondence,
  update documentation, and commit task-owned changes.

Use theorem-driven RED/GREEN and the subagent-driven workflow. Each worker owns
separate files. No correctness conclusion is a protocol axiom. No new dependency,
custom axiom, final sorry, or change to Model/Formula semantics is permitted.

## Validation

The full default build passes (58 jobs). Guarded axiom audits cover CM⇒SW⇒CW,
CW's strict-chain conflict-depth proof, all six SW/CW correctness endpoints,
and the common finite witness. Every guarded proof uses only the standard Lean
axioms. Independent review checked the coherence definitions, witness orientation,
strict-chain induction, differing learner depths, and liveness assumptions.
